[phases.build]
cmds = ["npm run build"]

[phases.start]
cmds = ["npm run start"]

[static]
# Install system dependencies
aptPkgs = ["ffmpeg", "python3", "python3-pip"]

# Install yt-dlp
[phases.build.cmds]
cmds = [
  "npm run build",
  "pip3 install yt-dlp"
]
